# Generated by claude-3-7-sonnet-latest

from typing import Optional, Annotated, List, Dict
from pydantic import BaseModel, Field
import operator

class State(BaseModel):
    """State class for report generation workflow."""
    
    topic: Optional[str] = Field(
        default=None, 
        description="The report topic provided as input; initial entry point for report generation."
    )
    
    sections: List[str] = Field(
        default_factory=list,
        description="List of up to 4 section titles generated based on the report topic; used as input list to the worker node for section content generation."
    )
    
    processed_sections: Annotated[List[str], operator.add] = Field(
        default_factory=list,
        description="List used to accumulate the content generated by the worker for each report section; stores the synthesized content for all sections, which may include multiple versions per section."
    )