# Generated by claude-3-7-sonnet-latest
from typing import Optional
from pydantic import BaseModel, Field

class State(BaseModel):
    """State class for joke generation and evaluation graph."""
    
    topic: str = Field(default="", description="The topic for the joke, provided when the graph is invoked.")
    joke: str = Field(default="", description="The joke generated based on the topic.")
    is_funny: bool = Field(default=False, description="Result of the joke evaluation, set to True or False at random.")