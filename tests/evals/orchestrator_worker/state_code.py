# Generated by claude-3-7-sonnet-latest

from typing import Optional, Annotated, List
from pydantic import BaseModel, Field
import operator

class State(BaseModel):
    """State class for report generation workflow."""
    
    topic: Optional[str] = Field(
        default=None, 
        description="The report topic provided upon invocation. Required for generating section titles and structuring the report content."
    )
    
    sections: List[str] = Field(
        default=[],
        description="A list of up to 4 section titles generated based on the report topic. Used as input to the worker node to generate content for each section."
    )
    
    processed_sections: Annotated[List[str], operator.add] = Field(
        default=[],
        description="The generated content for each section, one item per section. Populated by the worker node as it processes each item in sections."
    )