# Generated by claude-sonnet-4-20250514

from typing import Optional
from pydantic import BaseModel, Field


class JokesterState(BaseModel):
    topic: Optional[str] = Field(default=None, description="The topic for the joke as requested from the human user. This field stores the subject matter that the joke should be about.")
    
    joke: Optional[str] = Field(default=None, description="The generated joke content that will be displayed to the user. This field contains the actual joke text created based on the requested topic.")
    
    wants_another_joke: Optional[bool] = Field(default=None, description="Boolean flag indicating whether the user wants to hear another joke. This field stores the user's response when asked if they want another joke, and is used by the routing function tell_another to determine whether to continue with another joke or end the conversation.")